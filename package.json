{
  "name": "fanta-tracker",
  "version": "0.1.0",
  "private": true,
  "description": "Tracciamento dei cambi di mercato di una lega di Fantacalcio",
  "workspaces": [
    "backend"
  ],
  "scripts": {
    "analyze": "npm run analyze --workspace backend --",
    "test": "npm run test --workspace backend",
    "typecheck": "npm run typecheck --workspace backend",
    "build": "npm run build --workspace backend"
  },
  "engines": {
    "node": ">=20"
  }
}
